package com.zero.flow.domain.usecase.timer

import javax.inject.Inject

/**
 * Use case for resetting a timer session
 */
class ResetTimerUseCase @Inject constructor() {
    operator fun invoke() {
        // Reset timer - no business logic needed
    }
}